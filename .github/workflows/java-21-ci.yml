name: Java CI (Java 21)

on:
  push:
    branches: [ main, upgrade/java-21 ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Build
        run: mvn -B -DskipTests clean package
      - name: Run tests
        run: mvn test
