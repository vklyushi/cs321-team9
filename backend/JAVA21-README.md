# Java 21 Upgrade Notes

This project has been updated to target Java 21 (LTS).

Quick steps to build and test locally on Windows (PowerShell):

1. Install a Java 21 JDK (Temurin example):
```powershell
winget install --id EclipseAdoptium.Temurin.21.JDK -e
```

2. Start a PowerShell session and set `JAVA_HOME` for the session (adjust path if different):
```powershell
$env:JAVA_HOME = 'C:\Program Files\Eclipse Adoptium\jdk-21.0.9.10-hotspot'
$env:PATH = "$env:JAVA_HOME\bin;" + $env:PATH
java -version
mvn -v
```

3. Build and run tests:
```powershell
cd 'c:\Users\Forte\OneDrive\Desktop\GMU SE Project\gmu_workspace_v3\cs321-team9\backend'
mvn -DskipTests clean package
mvn test
```

Notes and recommendations:
- CI should be updated to use JDK 21 (set `JAVA_HOME` or use an action/matrix entry).
- We updated `pom.xml` files to set `<release>21>` and upgraded `maven-compiler-plugin` to `3.11.0`.
- I generated dependency reports under `reports/` in this module — check them for possible library upgrades.

If you'd like, I can open a Pull Request with these changes. If your environment requires a different JDK distribution, update the installation command accordingly.

---
Generated by automation on 2025-11-24
